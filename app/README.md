# VCNITY prototype

Installation steps, prerequisites, the demo script, and what each stage proves are all in the repo's main **[README.md](../README.md)**, under **Prototype app**.

Quick pointer if you're already set up: `python start.py` from this directory, or `python app/start.py` from the repo root.
